apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-python
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-python
  template:
    metadata:
      labels:
        app: hello-python
    spec:
      containers:
        - name: app # ชื่อ container
          image: orathaisu/hello-python:v2.0.0 # เปลี่ยน tag เป็น v2.0.0
          imagePullPolicy: Always 
          ports:
            - containerPort: 80
